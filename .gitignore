# Build artifacts (per-arch outputs produced by build.sh)
outputs/
rootfs-riscv/
rootfs-arm64/
kernel/build/
kernel/linux/
.cache/

# Stray kernel/bootloader artifacts that can leak to the repo root if a build
# is ever run from the working directory instead of inside the builder container
/Image
/fip.bin
/sophgo/

# CI / build artifacts that can land at the repo root
*.img.gz
*.zip
boot.vfat
rootfs.ext4
patch-report.txt
kernel-build.log
build.log

# Docker
.docker/

# Editor
*.swp
*.swo
*~
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Temporary
*.tmp
*.temp
*.log

# Compiled
*.o
*.ko
*.mod.c
*.order
*.symvers
